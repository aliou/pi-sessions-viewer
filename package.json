{
  "name": "pi-sessions-viewer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "prepare": "husky",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "wrangler": "^4.83.0"
  }
}
