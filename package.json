{
  "name": "pi-sessions-viewer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
